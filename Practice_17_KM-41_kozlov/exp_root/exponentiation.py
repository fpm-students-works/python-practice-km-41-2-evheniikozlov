def exp2(base):
    if not (isinstance(base, float) or isinstance(base, int)):
        raise ValueError("A base must be a float or an int")
    
    return base ** 2

def exp3(base):
    if not (isinstance(base, float) or isinstance(base, int)):
        raise ValueError("A base must be a float or an int")
    
    return base ** 3